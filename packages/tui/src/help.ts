/** Slash commands shared by the TUI `/help` line. */
export const SLASH_HELP = [
  "/help",
  "/ask /plan /agent",
  "/plan skip ID",
  "/todos /jobs /stop /check /doctor",
  "/config /yolo /lang",
  "/open /ide /steer /queue",
  "/resume /fork /fusion /traj /plugins",
  "/undo /apply /threads /quit",
].join("\n");
