# Skill catalog on demand

Skill plugins contribute `id` and `description` to the assembled `## skills` catalog. `SKILL.md` body must not be dumped into the system prompt at start. Load it with `read_skill`.
