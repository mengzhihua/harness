# Steer after the first tool

Start a turn that inspects the repo, then steer: stop after this tool, do not continue editing.

The trajectory must contain a `steer` event. AgentWorkspace should match the pre-steer files.
