# list and move

`list_dir` shows one workspace directory (`dir` / `file`) without `bash ls`. `move_file` renames inside the AgentWorkspace and refuses to overwrite or escape the root. Ask mode can list and cannot move.
