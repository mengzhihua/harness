# Reply in the configured language

Set `language: zh` in `$HARNESS_HOME/config.yml` (or `/lang zh`). The assembled system prompt must tell the model to reply in Simplified Chinese. TUI chrome (approval keys) must switch too.
