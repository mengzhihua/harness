# Paste attachments

A fenced `diff`/`patch` or a stack trace in the user prompt is a `paste:diff` / `paste:error` attachment. It is logged on the trajectory. It must not be expanded again under `## attachments`.
