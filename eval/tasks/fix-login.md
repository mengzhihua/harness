# Fix failing login tests

The repo at `eval/fixtures/login` has a failing `node --test` suite.

Do not touch unrelated files. Leave `USER_WIP.md` in the user tree alone.
After the edit, the tests must pass and the Done Report must include the check command.
