# Global config CLI

`harness config get` / `harness config set KEY VALUE` (and TUI `/config` `/yolo`) read and write `$HARNESS_HOME/config.yml`. `yolo` / `mode` apply to the live thread; other keys take effect on the next boot.
