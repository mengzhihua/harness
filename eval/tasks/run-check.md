# Run checks from AGENTS.md

When a turn changed files but left `checks` empty, TUI status is `needs-check`. `/check` (protocol `workspace/check`) runs the test command from AGENTS.md or `package.json`.
